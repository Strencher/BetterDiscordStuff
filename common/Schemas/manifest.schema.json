{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Plugin Manifest Schema",
    "type": "object",
    "properties": {
        "$schema": {
            "type": "string",
            "description": "The URL to the JSON schema (should be ../common/Schemas/manifest.schema.json)"
        },
        "name": {
            "type": "string",
            "description": "The name of the addon. It typically does not contain spaces, but it is allowed."
        },
        "version": {
            "type": "string",
            "description": "Version representing the current update level. Semantic versioning recommended.",
            "pattern": "^\\d+\\.\\d+\\.\\d+$"
        },
        "description": {
            "type": "string",
            "description": "A basic description of what the addon does."
        },
        "author": {
            "type": "string",
            "description": "The name of you the developer."
        },
        "authorId": {
            "type": "string",
            "description": "Discord snowflake ID of the developer. This allows users to get in touch.",
            "pattern": "^\\d{17,19}$"
        },
        "authorLink": {
            "type": "string",
            "description": "Link to use for the author's name on the addon pages.",
            "format": "uri"
        },
        "invite": {
            "type": "string",
            "description": "A Discord invite code, useful for directing users to a support server."
        },
        "donate": {
            "type": "string",
            "description": "Link to donate to the developer.",
            "format": "uri"
        },
        "patreon": {
            "type": "string",
            "description": "Link to the Patreon of the developer.",
            "format": "uri"
        },
        "source": {
            "type": "string",
            "description": "Link to the source on GitHub of the addon.",
            "format": "uri"
        },
        "website": {
            "type": "string",
            "description": "Developer's (or addon's) website link.",
            "format": "uri"
        },
        "changelog": {
            "type": "object",
            "description": "Changelog data shown via BetterDiscord's built-in changelog modal",
            "properties": {
                "title": {
                    "type": "string",
                    "description": "Title to show in the modal header. Defaults to \"What's New - <addon name>\" if omitted."
                },
                "subtitle": {
                    "type": "string",
                    "description": "Title to show below the main header. Defaults to the changelog date and version if omitted."
                },
                "date": {
                    "type": "string",
                    "description": "The date of the latest changelog (YYYY-MM-DD)",
                    "format": "date"
                },
                "video": {
                    "type": "string",
                    "description": "YouTube link or URL of a video file to use as the changelog banner"
                },
                "poster": {
                    "type": "string",
                    "description": "URL to use as the video freeze-frame poster",
                    "format": "uri"
                },
                "banner": {
                    "type": "string",
                    "description": "URL to an image to use as the changelog banner",
                    "format": "uri"
                },
                "blurb": {
                    "type": "string",
                    "description": "Text to show in the body of the changelog modal before the list of changes"
                },
                "changes": {
                    "type": "array",
                    "description": "List of changelog entries",
                    "items": {
                        "type": "object",
                        "properties": {
                            "type": {
                                "enum": ["added", "fixed", "improved", "progress"],
                                "description": "The type of change"
                            },
                            "blurb": {
                                "type": "string",
                                "description": "Text to show below this entry's title"
                            },
                            "title": {
                                "type": "string",
                                "description": "The title of this changelog entry"
                            },
                            "items": {
                                "type": "array",
                                "description": "List of changes",
                                "items": {
                                    "type": "string"
                                }
                            }
                        },
                        "required": ["type", "title", "items"],
                        "additionalProperties": false
                    }
                }
            },
            "additionalProperties": false
        }
    },
    "required": ["name", "version", "author"],
    "additionalProperties": false
}
